# Offline TTS-to-ASR Chain  

This recipe demonstrates an offline TTS-to-ASR chain using speechain toolkit. The process involves generating synthetic speech data using a pre-trained TTS model and then training an ASR model on this synthetic data. This approach can be beneficial for augmenting training data and improving ASR performance, especially in low-resource scenarios.

